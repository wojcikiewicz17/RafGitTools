name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:
    inputs:
      build_mode:
        description: "Wizard: choose which build stages to run"
        required: true
        default: all
        type: choice
        options: [all, build_only, tests_only, lint_only]
      run_release_variant:
        description: "Include release variants (requires signing secrets)"
        required: false
        default: false
        type: boolean
      gradle_args:
        description: "Extra Gradle args (example: --stacktrace --info)"
        required: false
        default: "--stacktrace"
        type: string

jobs:
  android-ci:
    name: Android CI Pipeline
    uses: ./.github/workflows/android-ci.yml
    secrets: inherit
    with:
      build_mode: ${{ inputs.build_mode || 'all' }}
      run_release_variant: ${{ inputs.run_release_variant || false }}
      allow_unsigned_release: false
      gradle_args: ${{ inputs.gradle_args || '--stacktrace' }}
